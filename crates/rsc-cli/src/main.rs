//! CLI entry point for the `RustScript` compiler.
//!
//! Parses command-line arguments and delegates to `rsc-driver` for compilation.
#![warn(clippy::pedantic)]

fn main() {
    println!("rsc: RustScript compiler");
}
